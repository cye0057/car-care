# 简历条目（可直接粘贴）

> 项目：**车管家 · 汽车维修保养服务平台**（Java 后端 + 车主端 H5）
> 数据出处见文末「数字速查表」，所有数字均来自 `document/jmeter/*.jtl` 实测，未做外推

---

## 一、按简历空间选用

### 精简版（2 行，空间紧张时用）

> 主导开发与性能优化：设计 Redis 缓存三件套（逻辑过期+互斥锁防击穿、空值缓存防穿透、TTL 随机抖动防雪崩）+ Cache Aside 延迟双删，JMeter 压测 5000 请求下数据库查询从 5001 次降至 1 次，读放大降低 99.98%；设计 Redis+Lua 原子预减 + RabbitMQ 异步落库的秒杀链路，300 并发抢 20 库存压测 0 超卖 0 重复 0 丢失。

### 标准版（4-5 行，最常用）

> 主导设计与全链路实现，覆盖缓存、高并发秒杀、消息队列、地理检索、Feed 流、实时推送等技术点：
> - 针对门店详情读多写少场景设计 Redis 缓存：逻辑过期 + 互斥锁防击穿、空值缓存防穿透、TTL 随机抖动防雪崩，写路径 Cache Aside + 延迟双删保一致性；压测 5000 请求下数据库查询从 5001 次降至 1 次，读放大降低 99.98%，吞吐提升 2.0x
> - 秒杀链路 Redis+Lua 原子预减（库存判断/一人一券/扣减/记名四步合一）+ RabbitMQ 异步落库削峰，消费端手动 ack、唯一索引幂等、毒消息隔离；订单超时关闭用死信队列替代定时扫表，DB 零空转
> - 号段模式发号（双缓冲 + 90% 异步预取）替代 UUID/自增；设计关注 Feed 推拉结合，超阈值自动转拉模式，双游标（maxId+beginTime）滚动分页规避深分页
> - 门店热度榜由定时任务聚合订单/评价刷新 ZSet，查询 O(logN+M)；工单动态经 Redis Pub/Sub 扇出至各节点 WebSocket 会话，实现集群无关的实时通知
> - 图片上传接入阿里云 OSS（后端中转 + 类型/大小校验 + 按日期分片命名）；订单支付接入支付宝沙箱（wap 下单返回收银台 URL + 异步回调 rsaCheckV1 验签 + 按订单号幂等驱动状态机）
> - 用 JMeter 5.6.3 搭建压测体系，产出可量化优化闭环；300 并发抢 20 库存压测暴露「库存初始化丢 7 个请求」竞态并修复，0 超卖 0 重复 0 丢失

### 一句话版（简历摘要 / 自我介绍）

> 独立完成一个具备高并发场景的本地车主服务平台，以「读多写少缓存」「秒杀防超卖」「MQ 异步削峰」「推拉结合 Feed」为主线，每个技术点都配套 JMeter 压测数据与实测 bug 修复，形成可量化的性能优化闭环。

---

## 二、按 JD 关键词挑选（单技术点独立成条）

**如果你投的岗位 JD 强调某一方向，只挑对应条目：**

| JD 关键词 | 用这条 |
|---|---|
| Redis 缓存 / 缓存一致性 | 缓存三件套 + 延迟双删，99.98% 读放大降低 |
| 高并发 / 秒杀 / 超卖 | Redis+Lua 原子预减 + 三道闸门，300 并发 0 超卖 |
| 消息队列 / 异步 / 削峰 | RabbitMQ 手动 ack + 唯一索引幂等 + 死信延迟关单 |
| 分布式 ID / 号段 | 号段模式双缓冲 + 90% 预取 |
| 推送 / WebSocket / 实时 | Redis Pub/Sub 扇出各节点 WebSocket，集群无关 |
| Feed 流 / 社交 / 分页 | 推拉结合 + 双游标滚动分页 |
| 地理信息 / LBS | Redis GEO（GeoHash+ZSet）+ 启动装载 + 写路径实时同步 |
| 性能优化 / 压测 | JMeter 体系 + 压测暴露竞态并修复（13→20） |
| 前端 / 全栈 | 车主端 H5 全站（Vue3+Vant）打通前后端全链路 |
| 文件上传 / 云存储 | 阿里云 OSS 后端中转：登录鉴权复用、类型/大小校验、日期分片、URL 落库 |
| 支付 / 第三方对接 | 支付宝沙箱 wap 下单 + 异步回调验签 + 订单幂等置支付 |

### 单点完整版

**缓存一致性**
> 针对读多写少场景设计 Redis 缓存：逻辑过期 + SETNX 互斥锁防击穿、空值缓存防穿透、TTL 随机抖动防雪崩，写路径 Cache Aside + Lua 安全解锁 + 500ms 延迟双删解决「写事务回滚前回填旧值」竞态。JMeter 压测 5000 请求下数据库查询从 5001 次降至 1 次（99.98%），吞吐从 508 提升至 1018 QPS。

**秒杀防超卖**
> 设计 Redis+Lua 原子脚本将「查库存+查重复+预减+记名」四步合一在 Redis 单线程内串行执行，天然无锁且一次往返；唯一索引 (coupon_id,user_id,type) 与 DB 条件扣减 `stock>0` 构成二、三道闸门；落库失败自动补偿回滚 Redis。冷启动 300 并发抢 20 库存，成功恰好 20、DB 剩余 0，0 超卖 0 重复。

**消息队列异步化**
> 抢券成功后发号 + 投递消息立即返回，消费者手动 ack 落库实现削峰；幂等靠唯一索引冲突后回补库存并吞掉，毒消息 nack 不进重队列做隔离；订单超时关闭用队列级 TTL + 死信交换机替代定时任务扫全表，DB 零空转，关单精度等于投递时刻加 TTL。

**Feed 推拉结合**
> 小账号发帖写扩散进粉丝收件箱 ZSet，粉丝数超阈值（默认 3000）自动切拉模式；查询合并收件箱与 DB 拉取，按 id 降序去重；分页用双游标 (maxId, beginTime) 替代 offset，规避 ZSet 深分页 O(N) 与边界重叠漏数据。

**实时通知（集群）**
> 工单动态经 Redis Pub/Sub 发布到 `ws:notify`，各节点订阅后仅投递本节点持有的 WebSocket 会话，新增节点自动加入、无需会话粘滞配置；握手 JWT 鉴权、30s 心跳保活、3s 断线重连。

**文件上传（阿里云 OSS）**
> 设计统一图片上传接口 `POST /api/upload`：登录拦截复用鉴权，服务端校验图片类型与 ≤5MB，按 `car-care/yyyy/MM/uuid.ext` 日期分片命名存 OSS，返回公网 URL 落库；管理端（门店封面/项目图）与车主端（笔记晒图/头像）共用一个接口，前端只存 URL 不接触字节流。

**第三方支付（支付宝沙箱）**
> 订单支付接入支付宝 wap 下单：下单返回沙箱收银台跳转 URL，支付结果只信异步回调——`/api/notify/alipay` 用 `rsaCheckV1` 验签防伪造通知，`TRADE_SUCCESS` 才按 `out_trade_no` 幂等驱动订单状态机 1→2（重复通知不重复处理）；回调路径放行登录拦截。

---

## 三、STAR 项目描述模板（面试口述用）

**Situation**：以维修保养为核心业务域做本地车主服务平台，基础 CRUD 跑通后需要叠加高并发场景，目标不是堆技术，而是每个技术点都能量化证明。

**Task**：让「读多写少」「瞬时写峰」「实时推送」三个典型场景都有可验证的优化方案，而不是纸上谈兵。

**Action**：
- 缓存：逻辑过期扛住绝大多数击穿（200 并发只 2 次回源），SETNX 互斥锁 + 物理 TTL 兜底，延迟双删解决回滚竞态
- 秒杀：Lua 单线程串行化免锁，三道闸门防超卖，MQ 异步落库把写峰削掉，死信队列替代扫表关单
- Feed：按粉丝阈值推拉切换，双游标分页
- 通知：Redis Pub/Sub 扇出，让 WebSocket 与节点拓扑解耦

**Result**：
- 数据库读放大降低 99.98%（5001→1 次 SELECT），同请求数吞吐 2.0x
- 300 并发抢 20 库存：0 超卖、0 重复、0 丢失
- 压测反向暴露了一个功能测试测不出来的 bug：库存初始化用互斥锁导致冷启动丢 7 个请求（成功数 13 而非 20），改成 `setIfAbsent` 原子装载后 20/20

**为什么这条 Action 值得讲**：压测不是走流程，而是真的把并发竞态逼出来了。

---

## 四、数字速查表（面试前背这个，别记错）

| 数字 | 含义 | 出处 |
|---|---|---|
| **99.98%** | 5000 请求下 DB SELECT 从 5001 次降到 1 次 | `baseline-no-cache.jtl` + MySQL general log |
| **2.0x** | 同 5000 请求吞吐 508 → 1017.7 QPS，总耗时 9.84s → 4.91s | `cached-hot.jtl` |
| **200 → 2** | 删 key 后 200 并发击穿，只有 2 次回源 | `breakdown.jtl` + general log |
| **13 → 20** | 库存初始化 bug 修复前后成功数，差 7 | `seckill-run.jtl` / `seckill-final.jtl` |
| **0 超卖 0 重复 0 丢失** | 300 并发抢 20 库存，DB 剩余 0，领券记录 20 条去重用户 20 | `seckill-final.jtl` + DB 核对 |
| **1001~1041** | 号段订单号连续递增无冲突 | DB 核对 |
| **7.02 / 8.42 km** | 附近门店距离，与 `redis-cli GEORADIUS` 原值一致 | 手工核对 |
| **3000** | Feed 推/拉模式切换的粉丝数阈值 | `carcare.feed.push-follower-threshold` |
| **500ms** | 延迟双删间隔 | `CacheHelper` |
| **10s / 30min** | 关单 TTL（演示 / 生产） | `carcare.order.close-ttl-ms` |
| **100** | 号段步长 | `t_seq_alloc` |
| **372 模块** | 前端生产构建 0 错误 | `vite build` |
| **91 类 / 4522 行** | 后端规模 | 源码统计 |
| **14 表** | 数据库规模 | `car_care.sql` |

---

## 五、不要说的话（诚实边界）

面试最容易翻车的是夸大。这些别主动说：

- ❌ 「QPS 提升了 10 倍」——本地 MySQL 主键查询本身 <1ms，RT 差异只有毫秒级。真实收益是**数据库读保护**，别说 RT
- ❌ 「扛住了上万并发」——最大压测是 300 并发。就说 300 并发
- ❌ 「做到了最终一致性」——延迟双删是近似手段，是精度与成本的折中，不是强一致
- ❌ 「生产环境部署过」——单机本地部署，MySQL/Redis/RabbitMQ 都在本机或虚拟机，没有真实集群
- ❌ 「缓存零不一致」——逻辑过期就是允许秒级不一致，换的是零等待

被问到容量问题时，主动给边界比硬撑可信度高得多。

---

## 六、技术栈清单（简历技能栏）

**后端**：Spring Boot 3.5 / JDK 17 / Spring MVC / 事务 / AOP / 拦截器鉴权
**ORM**：MyBatis-Plus 3.5.12（分页、条件构造器、逻辑删除）
**缓存**：Redis 8 / spring-data-redis / Redisson 3.34（Lua 脚本、SETNX 互斥锁、GEO、ZSet、Pub/Sub）
**消息队列**：RabbitMQ 3.13.7（交换机、死信交换机、队列级 TTL、手动 ack、消费者幂等）
**实时通信**：WebSocket（`@ServerEndpoint` + `ServerEndpointExporter`）
**前端**：Vue 3 / Vite / Pinia / Element Plus（管理台）/ Vant（车主端 H5）/ axios
**测试与压测**：JUnit 5 / JMeter 5.6.3（参数化脚本、CSV Data Set、断言）
**工具**：Hutool 5.8.40 / Lombok / JJWT 0.12.6 / springdoc 2.8.9（OpenAPI 在线文档）
**第三方**：阿里云 OSS SDK 3.17（图片上传）、支付宝开放平台 SDK 4.40（沙箱支付）
**数据库**：MySQL 8（唯一索引幂等、条件 UPDATE 防超卖）
**运维**：Windows 服务、Nginx/Vite 代理、多环境配置
